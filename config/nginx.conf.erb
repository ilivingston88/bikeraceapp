worker_processes auto;

events { worker_connections 1024; }

http {
    server {
        listen <%= ENV["PORT"] %>;

        location / {
            root /app;
            index index.html;
            try_files $uri $uri/ =404;
        }
    }
}