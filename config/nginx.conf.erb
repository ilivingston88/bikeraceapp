worker_processes auto;

events { worker_connections 1024; }

http {
    server {
        listen <%= ENV["PORT"] %>;

        location / {
            root /app; # Adjust this if your files are in a directory 
            index index.html;
            try_files $uri $uri/ =404;
        }
    }
}