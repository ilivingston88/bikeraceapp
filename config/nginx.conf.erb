worker_processes 1;
daemon off;

events {
    worker_connections 1024;
}

http {
    server {
        listen <%= ENV["PORT"] %>;
        server_name _;
        root /app/public;

        location / {
            try_files $uri $uri/ /index.html;
        }
    }
}